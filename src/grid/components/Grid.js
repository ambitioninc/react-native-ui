import React from 'react-native';
import {Text, View} from 'react-native';


const Grid = React.createClass({
    render() {
        return (
            <View>
                <Text>
                    React Native Grid
                </Text>
            </View>
        );
    }
});

export default Grid;
